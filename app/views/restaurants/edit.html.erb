<%# edit path %>
<div class="max-w-xl mx-auto py-12 px-4">
  <h1 class="text-3xl font-bold mb-8 text-center text-gray-900">Edit Restaurant</h1>
  <%= form_with model: @restaurant_form, url: restaurant_path(@restaurant_form.restaurant), method: :patch, class: "space-y-6" do |f| %>
    <div>
      <%= f.label :name, "Restaurant Name", class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :name, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
    </div>
    <div>
      <%= f.label :address, class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :address, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
    </div>
    <div>
      <%= f.label :phone, class: "block text-sm font-medium text-gray-700" %>
      <%= f.telephone_field :phone, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
    </div>
    <div>
      <%= f.label :website, class: "block text-sm font-medium text-gray-700" %>
      <%= f.url_field :website, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500" %>
    </div>
    <div class="pt-4">
      <%= f.submit "Update Restaurant", class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md transition" %>
    </div>
  <% end %>
  <div class="mt-8 text-center">
    <%= link_to "Back to Restaurant", restaurants_path, class: "text-blue-600 hover:text-blue-800" %>
  </div>
  <div class="mt-4 text-center">
    <%= link_to "Delete Restaurant", restaurants_path, method: :delete, data: { confirm: "Are you sure you want to delete this restaurant?" }, class: "text-red-600 hover:text-red-800" %>
  </div>
</div>
<%# end %>
<%# Note: Ensure you have the necessary routes set up in config/routes.rb for restaurant_path and other paths used here. %>
<%# This code assumes you have a RestaurantForm object that handles the form data %>
<%# and that the @restaurant_form instance variable is set in your controller. %>
<%# The form uses the PATCH method to update the restaurant details %>
<%# and includes a link to delete the restaurant with a confirmation prompt. %>
<%# The styling and structure are consistent with the rest of your application. %>
<%# Ensure you have the necessary CSS classes defined in your stylesheets %>
<%# for the form elements and buttons to match your design. %>
<%# This code is designed to be placed in the edit.html.erb view file for the Restaurants controller %>
<%# and will render a form for editing an existing restaurant's details. %>
<%# Make sure to test the form functionality and validation in your Rails application %>
<%# to ensure it works as expected. %>
<%# This code is part of a Ruby on Rails application and is used to render a form for editing a restaurant's details. %>					